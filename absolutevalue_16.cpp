//to see absolute value.
#include<iostream>
using namespace std;
int main()
{
    int a;
    cout << "Enter any number to see its absolute value : ";
    cin >> a;
    if(a<0)
    cout << -a;  //absolute value : if the input number is negative output will be positive.
    //else
    //cout << a;

}